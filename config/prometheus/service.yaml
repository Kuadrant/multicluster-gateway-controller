apiVersion: v1
kind: Service
metadata:
  name: metrics
  namespace: system
spec:
  selector:
    control-plane: controller-manager
  ports:
    - protocol: TCP
      name: metrics
      port: 8080
      targetPort: metrics